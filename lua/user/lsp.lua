-- local M = {}

-- M.config = function()
--   local lspinstall_ok, lspinstall = pcall(require, "lspinstall")
--   local lspconfig_ok, lspinstall = pcall(require, "lspinstall")
--   if not lspconfig_ok or not lspinstall_ok then
--     return
--   end

--   local servers = 

--   for _, server in pairs(servers) do
--     .setup{}
--   end
-- end

-- return M
